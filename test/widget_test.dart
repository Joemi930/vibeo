// Tests réels ajoutés dans la tâche T5 (miroir de lib/). Ce fichier de base
// généré par `flutter create` est remplacé pour ne pas référencer l'ancien
// widget supprimé.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — les tests de la Phase 1 sont ajoutés en T5', () {
    expect(true, isTrue);
  });
}
